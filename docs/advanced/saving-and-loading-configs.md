# Saving & Loading Configs

Hypster no longer ships built-in `save()` or `load()` helpers. Store the replayable params returned by `instantiate_with_params()` using your own JSON, database, tracker, or artifact system.

See [Serialization](../reproducibility/serialization.md).
