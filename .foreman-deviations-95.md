# Issue 95 deviations log

## Invented names

- Harness directory: `tests/jupyterlab_host/`.
- Sole notebook fixture: `branch_round_trip.ipynb`.
- Browser test: `test_jupyterlab_host.py`.
- Harness project name: `hypster-jupyterlab-host-harness`.
- Host dependency manifest/lock: `pyproject.toml` and `uv.lock` inside the harness directory.
- Kernel dependency input/lock: `kernel-requirements.in` and `kernel-requirements.lock`.
- CI workflow: `.github/workflows/jupyterlab-host.yml`.
- Temporary kernel name: `hypster-host`.
- Evidence artifact directory: `host-evidence/`.
- Evidence files: `setup.txt`, `kernel-install.txt`, `jupyterlab.log`, `versions.json`, `notebook-output.txt`, and failure-only `failure.png`.

## Granted guesses

- Python 3.13 is the single CI anchor for this JupyterLab 4 host job; the ticket asks for a current real host, not a Python-version matrix.
- Chromium is installed by Playwright from the harness lock and its exact runtime version is recorded.
- The sole notebook contains exactly two code cells: creation/import proof and the final `result.params` verification oracle.
- The frozen validation contract confirms the TDD seam: Chromium DOM event -> anywidget comm -> Python controller -> replacement DOM -> Python `result.params`.
- A visible `.hypster-status-error` or `.hypster-status-draft_error` is treated as a renderer/protocol failure and fails the run.
- A dedicated temporary Jupyter data directory and kernelspec are sufficient to prove the clean kernel boundary when the notebook also asserts and prints the installed `site-packages` import path.
- The kernel dependency lock adds the #82 upper bounds for anywidget, ipywidgets, and jupyterlab-widgets while resolving their exact current versions.
- Root pytest excludes `tests/jupyterlab_host/` from ordinary recursive collection; the locked host workflow invokes its test file explicitly.

## Extra actions

- None.

## Read-only exploration subagents

- None.
